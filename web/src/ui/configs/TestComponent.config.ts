import { createTestComponentConfig } from "@shared/configs";
import { FilterIcon } from "lucide-react";
import { TestComponent } from "@shared/components/TestComponent";

export default createTestComponentConfig(TestComponent, FilterIcon);
