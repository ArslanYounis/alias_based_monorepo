import { FilterIcon } from "lucide-react";
import { createFilterBarConfig } from "@shared/configs";
import { FilterBar } from "./FilterBar";

export const filterBarConfig = createFilterBarConfig(FilterBar, FilterIcon);
