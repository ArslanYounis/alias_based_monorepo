import { CaptionsIcon } from "lucide-react";
import { createTitleBarConfig } from "@shared/configs";
import { TitleBar } from "./TitleBar";

export const titleBarConfig = createTitleBarConfig(TitleBar, CaptionsIcon);
