import { createPhoneInputConfig } from "@shared/configs";
import { Mailbox } from "lucide-react-native";
import { PhoneInput } from "./PhoneInput";

export default createPhoneInputConfig(PhoneInput, Mailbox);
