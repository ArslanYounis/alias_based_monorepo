/* istanbul ignore file */
import { createTitleBarConfig } from "@shared/configs";
import { CaptionsIcon } from "lucide-react-native";
import TitleBar from "./TitleBar";

export default createTitleBarConfig(TitleBar, CaptionsIcon);
