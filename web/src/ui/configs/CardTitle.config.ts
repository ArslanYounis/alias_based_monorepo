import { createCardTitleConfig } from "@shared/configs";
import { HeadingIcon } from "lucide-react";
import { CardTitle } from "@shared/components/CardTitle";

export default createCardTitleConfig(CardTitle, HeadingIcon);
