/* istanbul ignore file */
import { createFilterBarConfig } from "@shared/configs";
import { Filter } from "lucide-react-native";
import FilterBar from "./FilterBar";

export default createFilterBarConfig(FilterBar, Filter);
