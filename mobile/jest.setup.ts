// @testing-library/react-native v13+ includes custom matchers automatically.
// No explicit setup import is required.
//
// Note: react-native-reanimated is mapped to a manual mock via
// moduleNameMapper in jest.config.js (its native Worklets module is not
// initialized under jest and otherwise crashes the whole suite on import).
