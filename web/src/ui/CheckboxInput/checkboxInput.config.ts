import { createCheckboxInputConfig } from "@shared/configs";
import { ListCheck } from "lucide-react";
import { CheckboxInput } from "./CheckboxInput";

export default createCheckboxInputConfig(CheckboxInput, ListCheck);
