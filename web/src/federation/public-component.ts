import { Layout } from "@/ui/Layout";
import { CustomDrawer } from "@/ui/CustomDrawer";

/**
 * Direct component exports for federation (without configs)
 * These components are exported as-is for direct consumption
 */
export { Layout, CustomDrawer };
