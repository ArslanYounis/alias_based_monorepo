import { transformRendererConfigs } from "@shared/utils/transform-renderer-config";
import exposeFormComponents from "./expose.form-components";

export default transformRendererConfigs(exposeFormComponents);
