import { createPhoneInputConfig } from "@shared/configs";
import { MailboxIcon } from "lucide-react";
import { PhoneInput } from "./PhoneInput";

export default createPhoneInputConfig(PhoneInput, MailboxIcon);
