import { createCurrencyConfig } from "@shared/configs";
import { CurrencyIcon } from "lucide-react";
import { CurrencyInput } from "./CurrencyInput";

export default createCurrencyConfig(CurrencyInput, CurrencyIcon);
